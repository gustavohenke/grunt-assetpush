"use strict";

module.exports = function( grunt ) {
	grunt.registerMultiTask( "assetpush", "Append HTML tags for JS/CSS files", function() {
		console.log(this.files);
		this.files.forEach(function() {

		});
	});
};